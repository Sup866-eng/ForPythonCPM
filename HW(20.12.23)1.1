from math import sqrt

a = int(input())
b = int(input())
c = int(input())
if not type(a) is int or not type(b) is int or not type(c) is int:
    raise TypeError("Check if your input as an integer")
elif (a+b)<c or (c+b)<a or (a+c)<b:
    raise TypeError("This triangle can't exist")
else:
    p = a+b+c
    d = p/2
    s = sqrt(d*(d-a) + d*(d-b) + d*(d-c))
    print("Perimetre:" + str(p))
    print("Square:" + str(s))
